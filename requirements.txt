# ============================================================
# 发票报销系统 - 依赖清单
# 安装命令：pip install -r requirements.txt
# ============================================================

# 阿里云 OCR SDK (必须)
alibabacloud_ocr_api20210707

# PDF 文本提取（用于解析已有 PDF 文本层，非 OCR）
pdfplumber

# 二维码解码（用于扫描发票二维码）
pyzbar

# Excel 读写（生成报销单.xlsx 和 交通出行明细.xlsx）
openpyxl

# 图片处理（缩略图预览、二维码识别）
Pillow

# PDF 内部预览（使用 PyMuPDF 渲染 PDF 为图像，支持翻页）
PyMuPDF

# 拖拽文件支持（tkinterdnd2，若无法安装可忽略，程序仍可运行）
tkinterdnd2

# HTTP 请求（用于企业信息查询 API）
requests>=2.28.0

# ===== 新增：智谱 AI SDK =====
zai-sdk

# ============================================================
# 可选依赖（按需安装）
# ============================================================

# Windows 下通过 COM 接口导出 Excel 为 PDF（若不需要可注释）
# pywin32

# 打包为独立 exe 文件（若需要打包成可执行文件）
# pyinstaller